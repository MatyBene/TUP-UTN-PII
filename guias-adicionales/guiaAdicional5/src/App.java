public class App {
    public static void main(String[] args) {

    }
}

//  1. Crea un programa que declare un array estático de enteros y luego lo inicialice
//con los valores del 1 al 10. Luego, utiliza un bucle for para imprimir cada valor del
//array en una línea separada.
//  2. Crea un programa que declare un array estático de strings y luego lo inicialice con
//algunos nombres de frutas. Luego, utiliza un bucle for para imprimir cada valor
//del array en una línea separada.
//  3. Crea un programa que declare un array estático de doubles y luego lo inicialice
//con 5 valores. Luego, utiliza un bucle for para calcular la suma de todos los
//valores del array.
//  4. Crea un programa que declare un array estático de objetos de tipo "Persona"
//(una clase que has creado previamente con propiedades como nombre, edad,
//etc.). Luego, inicializa el array con algunas instancias de "Persona". Luego, utiliza
//un bucle for para imprimir los detalles de cada persona en el array.
//  5. Crea un programa que declare un array estático de enteros y luego lo inicialice
//con algunos valores. Luego, utiliza un bucle for para encontrar el valor máximo
//del array. (Sin utilizar la clase Math)
//  6. Crea un programa que declare un array estático de strings y luego lo inicialice con
//algunas palabras. Luego, utiliza un bucle for para encontrar la palabra más larga
//en el array. (Utilizar el método length() de la clase String)
//  7. Crea un programa que declare dos arrays estáticos de enteros del mismo tamaño
//y luego los inicialice con 5 valores a cada uno. Luego, utiliza un bucle for para
//calcular la suma de los valores en cada posición de los dos arrays y almacenar los
//resultados en un tercer array estático.
//  8. Crea un programa que declare un array estático de objetos de tipo "Coche" (una
//clase que has creado previamente con propiedades como marca, modelo, etc.).
//Luego, inicializa el array con algunas instancias de "Coche". Luego, utiliza un
//bucle for para imprimir los detalles de cada coche en el array, pero solo aquellos
//que tienen una marca determinada. (Utilizar método equals() de la clase String)
//  9. Escribe un programa que solicite al usuario que ingrese una cadena de caracteres
//y luego imprima en pantalla la misma cadena, pero en orden inverso. Por
//ejemplo, si el usuario ingresa la cadena "Hola, mundo!", el programa deberá
//imprimir "!odnum ,aloH". (Investigar y utilizar el método toCharArray() de la clase
//String)
//  10. Crea un programa que declare dos arrays estáticos de enteros del mismo
//tamaño y luego los inicialice con algunos valores. Luego, utiliza un bucle for para
//calcular el producto de los valores en cada posición de los dos arrays y almacenar
//los resultados en un tercer array estático.
//  11. Crea un programa que declare un array estático de objetos de tipo "Animal"
//(una clase que has creado previamente con propiedades como especie, tamaño,
//etc.). Luego, inicializa el array con algunas instancias de "Animal". Luego, utiliza
//un bucle for para imprimir los detalles de cada animal en el array, pero solo
//aquellos que son de una especie determinada.
//  12. Crea un programa que declare un array estático de enteros y luego lo inicialice
//con 5 valores. Luego, utiliza un bucle for para calcular el promedio de los valores
//en el array
//  13. Crea un programa que declare un array estático de objetos de tipo "Libro"
//(una clase que has creado previamente con propiedades como título, autor, etc.).
//Luego, inicializa el array con algunas instancias de "Libro". Luego, utiliza un bucle
//for para imprimir los detalles de cada libro en el array, pero solo aquellos cuyo
//autor es de un país determinado.
//  14. Crea un programa que declare un array estático de strings y luego lo inicialice
//con algunas palabras. Luego, utiliza un bucle for para eliminar todas las palabras
//que tienen menos de cuatro letras del array y luego imprime el nuevo array
//resultante.